<!DOCTYPE html>
<html lang="en">

<%- include('partials/head',{
    title : 'Footwear-Fusion',
  }) %>

<body>

    <%- include('partials/header') %>

    <main class="home-main">

       <article class="home-main-contenedor">
        <div class="container products-wrapper">
            <div class="row" style="gap:20px">
                <div class="col-12">
                    <h2 class="products-title">Resultados de la búsqueda: <%= keywords %></h2>
                </div>
                
                <% if (results.length) { %>
                    
                <% for(let product of results){ %>
                    <div class="home_card col-lg-4 col-md-6 mb-4">
                        <div class="card">
                            <div class="bg-image hover-zoom ripple" data-mdb-ripple-color="light">
                                <a href="/products/details/<%= product.id %>"><img class="w-100" src="/img/<%= product.image || 'default-image.png' %>" alt="img"></a>
                                <a href="/products/details/<%= product.id %>">
                                    <div class="mask">
                                        <div class="d-flex justify-content-start align-items-end h-100">
                                            <h5><span class="badge bg-danger ms-2"> <%= product.discount || "-" %> % OFF </span></h5>
                                        </div>
                                    </div>
                                    <div class="hover-overlay">
                                        <div class="mask" style="background-color: rgba(251, 251, 251, 0.15);"></div>
                                    </div>
                                </a>
                            </div>
                            <div class="card-body">
                                    <h5 class="card-title mb-3"> <%= product.name %> </h5>
                                <h6 class="mb-3">
                                    <strong style="font-size: 26px;">$<%= product.discount? product.price - product.price * product.discount / 100 : product.price %></strong>
                                    
                                </h6>
                                <div class="d-md-flex justify-content-md-end">
                                    <button type="button" class="btn btn-primary btn-rounded" data-mdb-ripple-init>
                                        <a class="text-light fs-6" href="/products/details/<%= product.id %>">Comprar</a>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                    <% } %>
                    <% }else{ %>
    
                        
                <div class="col-12">
                    <h2 class="products-title" style="color: tomato;">No hay productos que coincidan con esa búsqueda</h2>
                </div>
            </div>
            <% } %>
        </div>
       </article>

    </main>
    <%- include('partials/footer')  %>
    <%- include('partials/bootstrap') %>
    
</body>

</html>